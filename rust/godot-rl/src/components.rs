use godot::classes::class_macros::private::virtuals::Os::Vector2i;

pub struct Position {
    pub position: Vector2i,
}

pub struct Player;

pub struct BlocksMovement;

pub struct OpenState(pub bool);

pub struct Door;
